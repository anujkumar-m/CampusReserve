# Root-level gitignore
.DS_Store
Thumbs.db
*.log

# Only ignore local overrides for frontend (not .env itself - it has public VITE_ vars)
frontend/.env.local

# Keep backend secrets out of git (DB password, JWT secret, etc.)
backend/.env

# But keep example templates
!**/.env.example
